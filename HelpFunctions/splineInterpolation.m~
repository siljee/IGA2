function g = splineInterpolation(U_exact, knotVec, p, P)

greville_para = findGrevillePoints(knotVec,p);

x_para = linspace(0,1,10);
N = BsplineBasis(knotVec,p,x_para);
x_phys = P*N;
y_phys = U_exact(x_phys);

d = N'\y_phys'


figure(1)
hold on 

% The exact function
plot(x_phys,y_phys,'--k')

% The points 
%plot(greville_phys,d,'-*')

% The interpolated solution
plot(x_phys,N'*d,'r-')
plot(x_phys,y_phys,'--k')
% Verification
norm(U_exact(x_phys)'-N'*d)


% %%%%%%%%%%%%%%%%%%%%%%%%%%%% TEst av andre punkter %%%%%%%%%%%%%%%%%%%
% x_para2 = [0,0.1,0.3,0.35,0.39,0.4,0.7,0.8,0.99,1];
% x_phys2 = x_para2*(b-a)+a;
% 
% N = BsplineBasis(knotVec,p,x_para2);
% 
% norm(U_exact(x_phys2)'-N'*d)
% 
% % 
% 
% figure(2)
% hold on 
% 
% % The exact function
% plot(x_phys,U_exact(x_phys),'--k')
% plot(x_phys2,U_exact(x_phys2),'*m')
% 
% % The points 
% %plot(greville_phys,d,'-*')
% 
% % The interpolated solution
% plot(x_phys2,N'*d,'r-')
% % Verification
% norm(U_exact(x_phys2)'-N'*d)
% %end